import { auth } from './auth';
import { event } from './event';
import { session } from './session';
import { space } from './space';
import { user } from './user';

export const services = { auth, event, session, space, user };
