import { auth } from './auth';
import { channel } from './channel';
import { event } from './event';
import { file } from './file';
import { user } from './user';

export const services = { auth, channel, file, event, user };
