{
  "scripts": {
    "dev": "turbo run dev --parallel",
    "heroku-postbuild": "turbo run heroku-postbuild --filter ${FLOYD_APP_NAME}"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "turbo": "^2.3.1"
  },
  "engines": {
    "node": "20.11.1"
  },
  "packageManager": "npm@10.5.0"
}
